node_modules/
dist/
build/
coverage/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
.vscode/
.idea/
*.pid
tmp/

# uploaded attachments — never committed, and on a server these must live
# OUTSIDE the deployment folder (set UPLOAD_DIR) so a deploy cannot wipe them
uploads/
